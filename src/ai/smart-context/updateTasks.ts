import { SmartContext } from '../../types'

export async function updateTasks(context: SmartContext) {
  // TODO: This function will update the tasks in the context based on the most recent messages
  return context
}
